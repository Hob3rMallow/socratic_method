# Third-party notices

`third_party/stb_image_write.h` is Sean Barrett's stb image writer and retains
the public-domain/MIT dual-license notice contained in that file. It is used
only by the optional visualization/research targets.

The optional `pred_fixup` executable links against the system's libtiff and
OpenMP runtime; their licenses are not reproduced here.
